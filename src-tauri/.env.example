# Copy this file to src-tauri/.env and fill in your real key.
# src-tauri/.env is already git-ignored (see .gitignore) - never commit real keys.

ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional - override the model without recompiling Rust.
# ANTHROPIC_MODEL=claude-sonnet-5
